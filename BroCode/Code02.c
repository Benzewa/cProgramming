//break will exit the loop 
//continue will skip current iteration
#include <stdio.h>
int main(void)
{
	/*
	for(int i = 0;i < 100; i++)
	{
		if(i == 2 || i == 3 || i == 4 || i == 5)
			continue;
		if(i == 50)
			break;
		printf("%d\n", i);
	}
	*/

	double Prices[] = {2.0, 2.5, 5.0, 7.5};
	printf("%lu BYTES\n",sizeof(Prices));

}
